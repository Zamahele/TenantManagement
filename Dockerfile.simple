# Simple Dockerfile using local build
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 80
EXPOSE 443

# Copy the already-built application
COPY ./publish .

# Entry point
ENTRYPOINT ["dotnet", "PropertyManagement.Web.dll"]