version: "3.9"

services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Your_password123
    ports:
      - "1433:1433"
    healthcheck:
      test: ["CMD-SHELL", "echo 'SELECT 1' | /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P $SA_PASSWORD"]
      interval: 10s
      timeout: 10s
      retries: 15
    volumes:
      - mssql_data:/var/opt/mssql
    networks:
      - appnet

  web:
    build:
      context: .
      dockerfile: PropertyManagement.Web/Dockerfile
    ports:
      - "8080:80"
      - "8443:443"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      # Example connection string for SQL Server in Docker Compose
      - ConnectionStrings__DefaultConnection=Server=db,1433;Database=YourDb;User Id=sa;Password=Your_password123;
    volumes:
      - ./aspnetapp.pfx:/https/aspnetapp.pfx:ro
    networks:
      - appnet

volumes:
  mssql_data:

networks:
  appnet:
    driver: bridge